# data/

Put local course files here. This folder is git-ignored, so they are never committed.

- `Walking.mat`: the Lab 1 sample recording. It enables the MATLAB-parity tests.
- `LabStepDet_2025.m`: the original lab script. It is used by `scripts/octave_parity.sh`.
- Your own Physics Toolbox recordings (`.csv`).
