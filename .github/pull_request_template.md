## What and why

<!-- What does this change, and why is it needed? Link an issue if there is one. -->

## How it was checked

<!-- Commands run, files tried in the dashboard, screenshots for UI changes. -->

## Checklist

- [ ] Branch is up to date with `main` (rebased, not merged)
- [ ] Each commit is atomic and passes `npm test` and `uv run pytest`
- [ ] The original algorithm is still bit-exact with MATLAB (or untouched)
- [ ] `docs/schema.md` is updated if validation checks changed
- [ ] No course files (`data/`) are included
